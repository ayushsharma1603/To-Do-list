<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Todo List</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#212121] min-h-screen text-white px-4 sm:px-6 lg:px-8 flex items-center gap-16  flex-col">
  <h1 class="text-center text-white-600 text-7xl mt-10"><%=title%></h1>
  <div class="w-3/5 bg-[wheat] py-10 rounded-2xl">
    <p class="text-center text-black font-medium text-2xl"><%=content%></p>
  </div>
  <% const file=title+".txt" %>
  <% const del =`delete/${file}` %>
  <% const back =`back/${file}` %>
  <div class="w-full flex items-center justify-between">
    <a href="<%=back%>" class="text-blue-500 inline-block mt-2">Go back</a>
    <a href="<%=del%>" class="text-blue-500 inline-block mt-2">Remove Task</a>
  </div>
</body>

</html>
